package main;

import java.io.BufferedWriter;
import java.io.FileWriter;
import java.io.IOException;
import java.io.InputStream;

public class MainClass {

	static int factor=0;
	public static void main(String[] args) {
		InputStream is = MainClass.class.getResourceAsStream("/file.txt");
		char current = 0;
		try {
			BufferedWriter bw=new BufferedWriter(new FileWriter("E:/SpringProject/output.txt",true));
			while (is.available() > 0) {
				int c = is.read();
				if ((c >= 65 && c <= 90)) {
					c -= 65;
					c = (c + factor) % 26;
					c += 65;
					current = (char) (c);
				} else if (c >= 97 && c <= 122) {
					c -= 97;
					c = (c + factor) % 26;
					c += 97;
					current = (char) (c);
				}
				System.out.print(current);
				bw.write(c);
			}
			bw.flush();
			bw.close();
		} catch (IOException e) {
			e.printStackTrace();
		}

	}

}
